# Polymarket 5分钟市场自动交易机器人 - 完整功能解读报告

---

## 一、系统概述

这是一个针对 Polymarket 5分钟 BTC/ETH UP/DOWN 市场的全自动交易系统。

**核心玩法：** Polymarket 每5分钟开一个市场，市场开始时 Chainlink 快照一个"Price to Beat"（PTB）价格。5分钟结束时，如果加密货币价格 ≥ PTB，则 UP 赢；< PTB 则 DOWN 赢。赔率在 0.49-0.51 之间浮动。

**核心策略：** 在市场开始60秒后分析，80秒时下注，270秒时平仓（不等结算）。

---

## 二、系统架构

```
┌─────────────────────────────────────────────────────┐
│                  auto_bot_v3.py                      │
│              （主交易引擎 - 调度中心）                  │
│                                                      │
│  ┌──────────┐  ┌──────────┐  ┌──────────────────┐   │
│  │ 市场扫描  │→│ AI 分析   │→│ 下注执行 + 通知   │   │
│  │ 每2秒轮询 │  │ 60秒触发  │  │ Polymarket CLI   │   │
│  └──────────┘  └──────────┘  └──────────────────┘   │
└─────────────────────────────────────────────────────┘
         ↕                              ↕
┌─────────────────┐          ┌──────────────────────┐
│ polymarket_api   │          │ position_monitor.py   │
│ （市场数据采集）  │          │ （持仓监控 + 平仓）    │
│ Gamma API        │          │ 止盈: +15%            │
│                  │          │ 平仓: 结束前35-60秒    │
└─────────────────┘          └──────────────────────┘
         ↕                              ↕
┌─────────────────┐          ┌──────────────────────┐
│ ai_model_v2.py   │          │ Polymarket CLOB API   │
│ （AI评分模型）    │          │ （订单簿 + 下单）      │
│ 4大信号加权评分   │          │ gnosis-safe 钱包      │
└─────────────────┘          └──────────────────────┘
         ↕
┌─────────────────┐
│ binance_api.py   │
│ （实时行情数据）  │
│ K线 + 价格 + 量  │
└─────────────────┘
```

---

## 三、核心模块详解

### 3.1 auto_bot_v3.py — 主交易引擎

**职责：** 整个系统的调度中心，负责市场发现、分析触发、下注执行。

**时间线（每个5分钟市场）：**
```
0s    市场开始（Chainlink快照PTB）
2s    机器人发现新市场（每2秒轮询）
60s   触发AI分析（等待1分钟积累数据）
~68s  分析完成，决策是否下注
~70s  执行下注（如果满足条件）
270s  平仓（由position_monitor.py管理）
300s  市场结束
```

**关键类：MarketTracker**
- `update_markets()`: 每2秒调用 Gamma API 获取当前 BTC/ETH 5分钟市场
- `check_analysis_trigger()`: 检查市场是否已运行60-80秒，触发分析
- `analyze_and_trade()`: 核心决策函数，调用AI分析并执行下注
- `cleanup()`: 清理已结束的市场数据（结束后120秒删除）

**PTB获取策略（三层降级）：**
```
1. Gamma API 直接获取 eventMetadata.priceToBeat
   ↓ 失败
2. Playwright 无头浏览器从页面提取
   ↓ 失败
3. Binance K线近似（市场开始时的开盘价）
```

**容错机制：**
- 连续错误计数器（max_errors=10），超过则停止
- EPIPE错误特殊处理（Playwright管道断裂）
- 每次错误后等待5秒再继续

---

### 3.2 ai_model_v2.py — AI评分模型

**职责：** 分析市场数据，预测5分钟结束时价格相对PTB的位置。

**核心思想：** 不是预测"BTC会涨还是跌"，而是预测"5分钟结束时价格在PTB的哪一边"。

**四大信号：**

#### 信号1：价格位置（权重50/80）
```
核心指标：当前价格 vs PTB 的偏离度（用ATR归一化）

diff_in_atr = |当前价 - PTB| / ATR

> 3.0 ATR → 60分（几乎不可能翻盘）
> 2.0 ATR → 55分
> 1.5 ATR → 50分
> 1.0 ATR → 45分（40秒内翻盘概率很低）
> 0.5 ATR → 35分
> 0.3 ATR → 25分
> 0.15 ATR → 15分
≤ 0.15 ATR → 3分（几乎平盘，不值得赌）
```

**解读：** 这是最强信号。如果当前价格已经远离PTB（比如偏离了2个ATR），那么在剩余40秒内翻盘的概率极低。

#### 信号2：短期动量（权重30/80）
```
数据源：最近3根1分钟K线

计算：
- 3分钟价格变化方向
- EMA(3)斜率方向
- 最近1分钟微观动量方向

评分：
- 3分钟趋势 + 1分钟微观方向一致 → 25分
- 方向矛盾（可能反转）→ 10分
```

**解读：** 动量信号用于确认或否定价格位置信号。如果价格在PTB上方且动量向上，信号更强。

#### 信号3：RSI极端值（权重10/80）
```
RSI > 75 → 超买，可能回调（-1方向，10分）
RSI < 25 → 超卖，可能反弹（+1方向，10分）
RSI 65-75 → 轻微超买（5分）
RSI 25-35 → 轻微超卖（5分）
其他 → 中性（0分）
```

**解读：** RSI作为修正信号，在边界情况下可以翻转方向判断。

#### 信号4：成交量异常（权重10/80）
```
当前成交量 / 5分钟平均成交量

> 2.0 → 放量，趋势可能延续（10分）
> 1.5 → 高于平均（5分）
其他 → 正常（0分）
```

**解读：** 放量通常意味着趋势延续，增加信号可信度。

#### 综合决策逻辑
```python
if 位置分 >= 30:
    方向 = 位置方向
    if 动量确认:
        总分 = 位置分 + 动量分 + 量能分
    else:
        总分 = 位置分 - 动量分×0.5 + 量能分×0.3

elif 位置分 >= 10:
    if 动量确认:
        方向 = 位置方向
    else:
        方向 = 动量方向（信号矛盾，极不确定）

else:  # 几乎平盘
    方向 = 动量方向
    总分 = 动量分 + 量能分

置信度 = min(max(总分 / 80, 0), 1.0)
```

---

### 3.3 ai_analyze_v2.py — 决策与执行层

**职责：** 连接AI模型和交易执行，包含下注条件判断和仓位管理。

**下注条件（三重门槛）：**
```python
should_bet = (
    confidence >= 0.85    # 置信度 ≥ 85%
    and ev > 0.5          # 期望值 > 0.5
    and target_odds < 0.85  # 赔率 < 0.85
)
```

**期望值（EV）计算：**
```
EV = confidence × (1/odds - 1) - (1 - confidence)

示例：
  置信度90%, 赔率0.50:
  EV = 0.9 × (2.0 - 1) - 0.1 = 0.9 - 0.1 = 0.8 ✅

  置信度60%, 赔率0.50:
  EV = 0.6 × (2.0 - 1) - 0.4 = 0.6 - 0.4 = 0.2 ❌ (< 0.5)
```

**Kelly仓位管理（1/4 Kelly）：**
```
Kelly公式: f = (p×b - q) / b
使用1/4 Kelly（保守策略，适合5分钟高噪音市场）

仓位映射：
  kelly_quarter < 0.10  → 3份（信号弱）
  kelly_quarter < 0.21  → 5份（正常，conf≈85%）
  kelly_quarter < 0.23  → 7份（较强，conf≈90%）
  kelly_quarter < 0.245 → 8份（强，conf≈95%）
  kelly_quarter ≥ 0.245 → 10份（极强，conf≈98%+）

安全约束：
  余额 < $20: 最大10份
  余额 < $50: 最大余额×20%
  余额 ≥ $50: 最大余额×10%
  硬约束: 3-10份
```

**下注执行流程：**
```
1. 查询余额（polymarket clob balance）
2. 获取中间价（CLOB midpoint API）
3. 计算Kelly仓位
4. 提交买单（polymarket clob create-order --side buy）
5. 记录到 bets.jsonl 和 positions.jsonl
6. 发送Telegram通知
```

---

### 3.4 trading_state.py — 交易状态管理

**职责：** 管理交易状态、冷却期、胜负统计。

**状态结构：**
```json
{
  "consecutive_losses": 0,    // 连续亏损次数
  "cooldown_remaining": 0,    // 冷却期剩余（期数）
  "last_bet_time": "...",     // 最后下注时间
  "last_bet_result": "win",   // 最后结果
  "total_bets": 130,          // 总下注次数
  "total_wins": 120,          // 总胜次数
  "total_losses": 10          // 总负次数
}
```

**冷却机制：**
```
下注成功 → 重置连败计数，清除冷却期
下注失败 → 连败+1，进入3期冷却（观望3个市场）
每次分析后 → 冷却期-1
```

**设计意图：** 连续亏损后强制观望，避免情绪化连续下注。

---

### 3.5 position_monitor.py — 持仓监控与平仓

**职责：** 独立进程，监控所有持仓，执行止盈和定时平仓。

**两大平仓触发：**

#### 触发1：止盈（+15%）
```
监控窗口：市场结束前80-100秒
条件：当前价格 ≥ 入场价 × 1.15
动作：立即卖出锁定利润
```

#### 触发2：定时平仓（结束前35-60秒）
```
监控窗口：市场结束前35-60秒
逻辑：
  1. 判断当前方向是否必输（is_losing）
  2. 获取订单簿流动性
  3. 智能选择平仓策略
```

**智能平仓（新增）：**
```
1. analyze_liquidity() → 分析订单簿流动性
2. calculate_liquidity_score() → 评分（0-10）
3. 根据评分选择策略：
   - ≥8.0: 流动性匹配（最优价格）
   - ≥6.0: 单价格（流动性充足）
   - <6.0: 多价格梯度（传统策略）
4. 失败回退到 try_sell_with_multiple_prices()
```

---

### 3.6 数据采集层

#### polymarket_api.py
```
get_current_markets():
  - 计算当前5分钟时间窗口的slug
  - 查询 Gamma API 获取市场数据
  - 返回: slug, coin, end_time, up_odds, down_odds
```

#### binance_api.py
```
get_klines(): 获取K线数据（1分钟级别，最近15根）
get_current_price(): 获取实时价格
get_24h_stats(): 获取24小时统计
```

#### playwright_ptb.py
```
get_price_to_beat_playwright():
  - 启动无头Chromium浏览器
  - 导航到Polymarket页面
  - 拦截不必要资源（图片、字体、分析脚本）
  - 从DOM提取Price to Beat
  - 关闭浏览器（避免内存泄漏）
  - 耗时约6-8秒
```

#### indicators.py
```
纯Python实现的技术指标：
  - EMA（指数移动平均）
  - RSI（相对强弱指标）
  - MACD（移动平均收敛散度）
  - Bollinger Bands（布林带）
  - ATR（平均真实波幅）
```

---

## 四、资金流转

```
钱包架构：
  主钱包(0xb37c) → 私钥控制
  Gnosis-safe(0xe614) → CLOB交易账户（下单用）
  Proxy钱包(0x4932) → 持有token
    └ Owner: Polymarket ProxyWalletFactory

下单流程：
  余额查询 → Kelly仓位计算 → CLOB create-order(buy) → token进入Proxy钱包

平仓流程：
  订单簿分析 → 选择价格 → CLOB create-order(sell) → USDC回到Gnosis-safe

⚠️ 已知限制：
  - 自动结算无法实现（Proxy钱包Owner是工厂合约）
  - 只能通过前端手动结算
  - v3策略通过提前平仓规避了这个问题
```

---

## 五、通知系统

```
Telegram Bot 直接推送：
  - 下注成功通知（币种、方向、置信度、EV、价格）
  - 平仓通知（入场价、出场价、盈亏）
  - 止盈通知
  - 止损通知
```

---

## 六、日志系统

```
logs/
├── decisions_v2.jsonl    # 每次AI分析的决策记录
├── bets.jsonl            # 每次下注记录
├── positions.jsonl       # 持仓记录
├── closed_positions.jsonl # 已平仓记录
├── trading_state.json    # 交易状态（胜负统计）
├── auto_bot.log          # 主机器人运行日志
├── position_monitor.log  # 持仓监控日志
└── bot_v3.log            # v3历史日志
```

---

## 七、风控体系

| 层级 | 机制 | 说明 |
|------|------|------|
| 信号质量 | 置信度≥85% | 只在高确定性时下注 |
| 期望值 | EV>0.5 | 确保正期望值 |
| 赔率保护 | odds<0.85 | 不买太贵的合约 |
| 仓位管理 | 1/4 Kelly | 保守仓位，3-10份 |
| 余额保护 | 最大10%余额 | 单次不超过总资金10% |
| 冷却期 | 连败后观望3期 | 避免情绪化连续下注 |
| 止盈 | +15%自动卖出 | 锁定利润 |
| 定时平仓 | 结束前30秒 | 不依赖结算 |
| 错误容忍 | 连续10次错误停止 | 防止异常状态持续运行 |

---

## 八、性能指标

**历史战绩（trading_state.json）：**
```
总交易: 130次
胜率: 92.3% (120胜/10负)
连续亏损: 0
冷却期: 0
```

**资源消耗：**
```
auto_bot_v3.py: ~76MB内存, 5.9% CPU
position_monitor.py: ~31MB内存, 8.7% CPU
总计: ~107MB, ~14.6% CPU
```

**延迟：**
```
市场发现: ~2秒（轮询间隔）
PTB获取: 6-8秒（Playwright）
AI分析: <1秒
下注执行: ~2秒
总延迟: ~12秒（从触发到下注完成）
```

---

## 九、技术亮点

1. **三层PTB降级策略**：API → Playwright → Binance近似，确保数据可用性
2. **ATR归一化**：用波动率标准化价格偏离，跨币种可比
3. **1/4 Kelly仓位**：数学最优的保守仓位管理
4. **提前平仓策略**：规避了Proxy钱包无法自动结算的架构限制
5. **Playwright按需启动**：每次用完关闭，避免内存泄漏和EPIPE
6. **流动性感知平仓**：基于订单簿实际流动性选择最优平仓价格

---

## 十、已知问题与改进空间

### 已知问题
1. **Playwright内存**：长时间运行可能导致内存缓慢增长
2. **静默崩溃**：机器人可能无日志地退出（需看门狗）
3. **结算限制**：Proxy钱包架构导致无法自动结算
4. **赔率窄**：5分钟市场赔率在0.49-0.51之间，利润空间小

### 改进空间
1. **WebSocket实时数据**：替代轮询，降低延迟
2. **多市场并行**：同时分析更多时间框架的市场
3. **机器学习模型**：用历史数据训练更精准的预测模型
4. **自动重启机制**：systemd或看门狗确保进程存活
5. **回测框架**：用历史数据验证策略参数

---

## 十一、文件清单

### 核心文件（正在使用）
| 文件 | 功能 | 行数 |
|------|------|------|
| auto_bot_v3.py | 主交易引擎 | ~420行 |
| position_monitor.py | 持仓监控+平仓 | ~700行 |
| ai_analyze_v2.py | 决策+执行层 | ~200行 |
| ai_trader/ai_model_v2.py | AI评分模型 | ~220行 |
| ai_trader/polymarket_api.py | 市场数据采集 | ~70行 |
| ai_trader/binance_api.py | 币安行情数据 | ~60行 |
| ai_trader/playwright_ptb.py | 浏览器PTB获取 | ~100行 |
| ai_trader/indicators.py | 技术指标计算 | ~80行 |
| trading_state.py | 交易状态管理 | ~70行 |

### 辅助文件（历史/测试）
| 文件 | 说明 |
|------|------|
| auto_bot.py / auto_bot_v2.py | 旧版本 |
| ai_analyze.py | v1分析引擎 |
| monitor_bets.py | 独立通知脚本（已内置到v3） |
| backtest.py / backtest_accuracy.py | 回测工具 |
| 其他scanner/detector | 早期探索代码 |

---

**报告生成时间：** 2026-03-09 09:38
**代码版本：** v3
**分析深度：** 全部核心模块源码级解读
