# Polymarket 自动平仓逻辑改进方案

## 一、原有问题分析

### 1.1 失败率高的原因
- **单次尝试**：每个价格只尝试一次，失败就放弃
- **价格策略保守**：正常情况只尝试3个价格点
- **无重试机制**：网络波动或临时错误导致直接失败
- **价格梯度不足**：价格跳跃太大，错过最佳成交点

### 1.2 原有逻辑
```python
# 旧版 sell_position
- 单次调用 polymarket CLI
- 超时30秒
- 失败直接返回

# 旧版 try_sell_with_multiple_prices
- 正常情况：3个价格 [99%, 100%, 95%]
- 必输情况：6个价格
- 无等待，连续尝试
```

---

## 二、改进方案

### 2.1 核心策略：多层防护

```
第一层：单次调用重试（sell_position）
  └─ 最多3次重试
  └─ 每次失败后等待2秒

第二层：多价格梯度（try_sell_with_multiple_prices）
  └─ 正常：8个价格点
  └─ 必输：8个价格点
  └─ 每个价格调用第一层（3次重试）

第三层：价格尝试间隔
  └─ 每个价格失败后等待1秒
  └─ 让市场稳定，避免连续失败
```

### 2.2 改进后的 sell_position 函数

**功能：** 单次卖单提交，带重试机制

**参数：**
- `token_id`: 代币ID
- `size`: 数量
- `price`: 价格
- `max_retries`: 最大重试次数（默认3次）

**逻辑：**
```python
for attempt in range(max_retries):
    1. 调用 polymarket CLI 创建卖单
    2. 如果成功，立即返回
    3. 如果失败且还有重试机会：
       - 等待2秒
       - 重新尝试
    4. 所有重试失败后返回错误
```

**改进点：**
- ✅ 增加重试机制（3次）
- ✅ 失败后等待2秒再重试
- ✅ 处理超时和异常情况

---

### 2.3 改进后的 try_sell_with_multiple_prices 函数

**功能：** 多价格尝试平仓，根据情况选择策略

**参数：**
- `token_id`: 代币ID
- `size`: 数量
- `best_bid`: 最佳买价
- `current_price`: 当前价格
- `entry_price`: 入场价格
- `is_losing`: 是否必输

**策略一：正常平仓（is_losing=False）**

订单簿健康时：
```
价格梯度：[99%, 98%, 97%, 95%, 93%, 90%, 0.15, 0.10]
说明：从接近市场价开始，逐步降低，最后兜底价格
```

订单簿不健康时：
```
价格梯度：[99%, 98%, 97%, 95%, 93%, 90%, 0.15, 0.10]
基准：current_price（当前价格）
说明：使用当前价格作为基准，密集梯度
```

**策略二：必输平仓（is_losing=True）**

订单簿健康时：
```
价格梯度：[98%, 95%, 90%, 85%, 80%, 0.10, 0.05, 0.01]
说明：更激进的价格，快速止损
```

订单簿不健康时：
```
价格梯度：[95%, 90%, 80%, 70%, 0.10, 0.05, 0.01]
基准：current_price
说明：极端激进，不惜代价成交
```

**改进点：**
- ✅ 价格点从3-6个增加到8个
- ✅ 更密集的价格梯度（99%→98%→97%→95%→93%→90%）
- ✅ 每个价格失败后等待1秒
- ✅ 每个价格本身有3次重试机会（通过sell_position）
- ✅ 增加兜底价格（0.15, 0.10, 0.05, 0.01）

---

## 三、成功率提升计算

### 3.1 旧版成功率
```
假设单次成功率：70%
正常情况：3个价格 × 1次尝试 = 3次机会
总成功率：1 - (1-0.7)³ = 97.3%
```

### 3.2 新版成功率
```
假设单次成功率：70%
每个价格：3次重试
单价格成功率：1 - (1-0.7)³ = 97.3%

正常情况：8个价格 × 97.3%成功率
总成功率：1 - (1-0.973)⁸ = 99.9999%
```

### 3.3 极端情况
```
假设网络很差，单次成功率只有30%
每个价格：3次重试
单价格成功率：1 - (1-0.3)³ = 65.7%

8个价格尝试：
总成功率：1 - (1-0.657)⁸ = 99.98%
```

---

## 四、实际执行流程示例

### 4.1 正常平仓场景
```
时间点：市场结束前40秒
订单簿：健康（best_bid = 0.85）

执行流程：
1. 尝试价格 $0.84 (99%)
   └─ 第1次：失败（网络波动）
   └─ 等待2秒
   └─ 第2次：成功 ✅
   
总耗时：约4秒
结果：成功平仓
```

### 4.2 紧急平仓场景
```
时间点：市场结束前15秒
订单簿：不健康（best_bid = 0.10）
情况：必输（is_losing=True）

执行流程：
1. 尝试价格 $0.10 (98%)
   └─ 3次尝试全部失败
   └─ 等待1秒
   
2. 尝试价格 $0.095 (95%)
   └─ 3次尝试全部失败
   └─ 等待1秒
   
3. 尝试价格 $0.09 (90%)
   └─ 第1次：成功 ✅
   
总耗时：约10秒
结果：成功平仓（虽然价格不理想，但避免了更大损失）
```

### 4.3 极端情况
```
时间点：市场结束前5秒
订单簿：崩溃（best_bid = None）
情况：必输

执行流程：
1-7. 尝试所有高价格，全部失败
8. 尝试兜底价格 $0.01
   └─ 第1次：成功 ✅
   
总耗时：约15秒（可能超时）
结果：以极低价格成交，但避免了完全无法平仓
```

---

## 五、关键改进总结

| 项目 | 旧版 | 新版 | 提升 |
|------|------|------|------|
| 单价格重试次数 | 1次 | 3次 | 3倍 |
| 价格梯度数量 | 3-6个 | 8个 | 1.3-2.7倍 |
| 总尝试次数 | 3-6次 | 24次 | 4-8倍 |
| 理论成功率 | 97.3% | 99.9999% | 2.7% |
| 极端情况成功率 | 78.4% | 99.98% | 21.6% |
| 有等待机制 | ❌ | ✅ | - |
| 有兜底价格 | ❌ | ✅ | - |

---

## 六、风险与注意事项

### 6.1 潜在风险
1. **时间消耗**：最坏情况可能需要15-20秒
2. **价格滑点**：激进价格可能导致更大损失
3. **API限流**：频繁调用可能触发限流

### 6.2 缓解措施
1. **时间控制**：在市场结束前45-60秒开始平仓
2. **价格保护**：正常情况使用保守价格
3. **重试间隔**：等待1-2秒避免限流

---

## 七、建议

### 7.1 监控指标
- 平仓成功率
- 平均尝试次数
- 平均耗时
- 价格滑点

### 7.2 后续优化
1. 根据历史数据动态调整价格梯度
2. 增加订单状态检查（确认是否真的成交）
3. 考虑使用 WebSocket 实时订单簿
4. 增加平仓日志，分析失败原因

---

## 八、部署建议

1. **测试环境验证**：先在小额测试
2. **逐步上线**：先用保守策略，观察效果
3. **监控告警**：设置平仓失败告警
4. **回滚准备**：保留旧版代码备份

---

**修改文件：** `/root/.openclaw/workspace/polymarket-arb-bot/position_monitor.py`

**生效方式：** 重启 position_monitor.py 进程
